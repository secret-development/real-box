<h1 class="title-page">
  <%= link_to "Settings", settings_path %> → Districts
</h1>
<%= flash_msg(flash[:notice]) %>
<% @cities.each do |c| %>
  <div class="city-block-d">
    <h3 class="city-name-d"><%= c.name %>:</h3>  
    <div class="districts-b">
      <% c.districts.each do |d| %>
        <div class="district-b">
          <span class="district-t">
            <%= d.title %>
          </span>
          <span class="actions-with-d">
            <%= link_to edit_icon, edit_district_path(d), :rel => "tooltip", :title => "Edit" %>
            <%= link_to delete_icon, d, :method => :delete, :confirm => "Are you sure?", :rel => "tooltip", :title => "Remove (undesirable)" %>
          </span>
        </div>
      <% end %>
    </div>
  </div>
<% end %>